{
  "name": "agency-reports",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run check && tsx scripts/build-reports.ts",
    "sync": "tsx scripts/sync.ts",
    "pull": "tsx scripts/pull.ts",
    "dataform:sync": "bash scripts/dataform-sync.sh",
    "check": "tsx scripts/check-source-tables.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
